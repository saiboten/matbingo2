# Database. For local development use a database on this machine (see "Local database" in the README):
DATABASE_URL="postgresql://matbingo:matbingo@localhost:5432/matbingo_dev"
# Production looks like this and belongs in Vercel and in a separate, git-ignored .env.prod:
# DATABASE_URL="postgresql://user:password@host:port/database?sslmode=verify-full"

# Better Auth
BETTER_AUTH_SECRET="your-secret-key-here-min-32-characters-long"
BETTER_AUTH_URL="http://localhost:3000"

# Google OAuth
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-google-client-secret"
